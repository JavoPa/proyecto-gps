// Propósito: Contiene las constantes de la aplicación
const IP = '192.168.1.6'
const PUERTO = '3000'
const RUTA = 'api/auth/login'

export default API_URL = `http://${IP}:${PUERTO}/${RUTA}`;


// evaluar usar dotenv para las variables de entorno
